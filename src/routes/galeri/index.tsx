import { component$ } from "@builder.io/qwik";
import { Pages } from "~/components/layouts/Pages";

export default component$(() => {
    return (
        <Pages>
            <h1>ini adalah halaman untuk galeri kegiatan</h1>
        </Pages>
    )
});